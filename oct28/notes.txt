git branch branchname
when create via github, still have to create locally wit git branch

git checkout branchname to switch to that branch
git push --set-upstream origin branchname

git branch -d branchname removes

git merge master(other branch)

master branch and dev branch and then seperate developer branches
master is the release branc
dev may not always work
master will always work

git tags give names at certain places

issues
